server.port=8080

# Descriptive User-Agent for Open Food Facts (recommended by their API guidelines)
fitcoach.http.user-agent=FitCoach/1.0 (fitness demo; contact: dev@localhost) Java

# Comma-separated allowed frontend origins for CORS.
# Set this in production to your deployed frontend URL(s), e.g.
# https://your-app.vercel.app,https://your-custom-domain.com
fitcoach.cors.allowed-origins=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173
